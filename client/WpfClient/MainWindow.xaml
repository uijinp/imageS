<Window x:Class="WpfClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Modern Draw Client" Height="720" Width="1080"
        WindowStartupLocation="CenterScreen" Background="#F0F2F5">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#106EBE"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>

        <!-- Drawing Area -->
        <Grid Grid.Column="0" Margin="10">
            <Border Background="White" CornerRadius="5">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" Opacity="0.1"/>
                </Border.Effect>
                <Canvas x:Name="drawCanvas" Background="Transparent" 
                        MouseDown="DrawCanvas_MouseDown" 
                        MouseMove="DrawCanvas_MouseMove" 
                        MouseUp="DrawCanvas_MouseUp"
                        ClipToBounds="True"/>
            </Border>
            
            <!-- Floating Toolbar -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20">
                <Button Content="Clear Canvas" Click="BtnClear_Click" Background="#D93025"/>
            </StackPanel>
        </Grid>

        <!-- Chat Area -->
        <Grid Grid.Column="1" Background="White" Margin="0,10,10,10">
            <Grid.Effect>
                <DropShadowEffect BlurRadius="10" Opacity="0.05"/>
            </Grid.Effect>
            
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Padding="15" BorderBrush="#EEE" BorderThickness="0,0,0,1">
                <StackPanel>
                    <TextBlock x:Name="txtTitle" Text="Room: Room1" FontSize="16" FontWeight="Bold"/>
                    <TextBlock x:Name="txtUser" Text="User: Nickname" FontSize="12" Foreground="#777" Margin="0,2,0,0"/>
                </StackPanel>
            </Border>

            <!-- Chat Log -->
            <ScrollViewer x:Name="scrollChat" Grid.Row="1" Margin="10">
                <StackPanel x:Name="chatPanel"/>
            </ScrollViewer>

            <!-- Input Area -->
            <Grid Grid.Row="2" Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBox x:Name="txtChatInput" Padding="5" KeyDown="TxtChatInput_KeyDown"
                         BorderThickness="1" BorderBrush="#DDD" VerticalContentAlignment="Center"/>
                
                <Button Grid.Column="1" Content="Send" Click="BtnSend_Click" Margin="5,0,0,0" Width="60"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
